// Test IDs for the home / landing feature. Naming follows the directive
// in ./auth.js (keys camelCase, values kebab-case `<feature>-<element>`).
// Canonical HOME lives in ./index.js — this file kept for reference only.

export const HOME = {
	heroCta: 'hero-cta-button',
	heroSecondary: 'hero-secondary-button',
};
